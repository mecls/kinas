// The screens (build-spec AC-9) are Home's fixtures: the usage readings with Claude's session at 97 %, the Convex and
// Hostinger stubs, and a projects root of two synthetic repositories — nothing of the captain's on a committed screen.
export { setup, teardown } from "./home.setup.ts";
