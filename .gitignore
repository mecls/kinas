# The store and its WAL files never enter git (R3)
*.sqlite
*.sqlite-*

node_modules/
dist/
target/
app/src-tauri/binaries/
app/src-tauri/gen/
spikes/
.DS_Store
*.log

# Planning documents and the names AC-10 looks for stay on this Mac: the repository is public
tasks/
scripts/private-names
