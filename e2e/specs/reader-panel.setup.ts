// The reader's panel (tasks/three-column-shell-build-spec.md) runs against the reader spec's own projects root:
// fixtures/reader copied into the run's data folder, external links logged rather than opened.
export { setup, teardown } from "./reader.setup.ts";
